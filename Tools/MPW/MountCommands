# This file is like State of the Project, except that it executes rather
# than just displays.  Be very careful when using this, because the commands
# you type here will be executed on everyone else’s machine, probably when
# you’re not available to watch over it.

